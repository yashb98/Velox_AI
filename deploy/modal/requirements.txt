# deploy/modal/requirements.txt
# Dependencies for SGLang Modal deployment
#
# Note: Most dependencies are installed in the Modal image definition.
# This file is for local testing and development.

# SGLang inference engine
sglang[all]>=0.3.0

# Core ML
torch>=2.1.0
transformers>=4.40.0
vllm>=0.4.0
accelerate>=0.27.0

# FlashInfer for efficient attention
flashinfer>=0.1.0

# HTTP client for API calls
httpx>=0.27.0

# FastAPI for local testing
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Modal SDK
modal>=0.64.0
